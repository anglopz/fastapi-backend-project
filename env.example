# FastAPI Application Environment Variables
# Copy this file to .env and update with your actual values
# NOTE: PostgreSQL is now REQUIRED (no SQLite fallback)

# ============================================
# Security Settings
# ============================================
# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256

# ============================================
# Database Settings (PostgreSQL - REQUIRED)
# ============================================
# PostgreSQL is now required (UUID and ARRAY types require PostgreSQL)
# For Docker Compose, use the service names: db, redis
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=fastapi_db

# ============================================
# Redis Settings
# ============================================
# Redis host and port (separate from URL)
# For Docker Compose: redis (service name)
# For local development: localhost
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# Mail/Email Settings (Optional - for notifications)
# ============================================
# SMTP server configuration for sending emails
# Leave empty if mail functionality is not needed
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@example.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_FROM_NAME=FastShip
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
USE_CREDENTIALS=True
VALIDATE_CERTS=True
